{
  "name": "voidforge-backend",
  "version": "1.0.0",
  "description": "VOIDFORGE Express API and admin server",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "lint": "node --check server.js && node --check config.js && node --check routes/subscriberRoutes.js && node --check routes/adminRoutes.js && node --check controllers/subscriberController.js && node --check controllers/adminController.js && node --check middleware/auth.js && node --check middleware/validate.js && node --check models/Subscriber.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.3.1",
    "helmet": "^7.1.0",
    "mongoose": "^8.4.3",
    "morgan": "^1.10.0",
    "nodemailer": "^6.9.14",
    "validator": "^13.12.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
